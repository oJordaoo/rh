{
  "name": "desktop",
  "root": "apps/desktop",
  "sourceRoot": "apps/desktop/src",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm --workspace desktop run serve"
      }
    }
  }
}
